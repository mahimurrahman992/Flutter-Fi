import 'package:flutter/material.dart';
/*

final ThemeData darkTheme = ThemeData(
  brightness: Brightness.dark,
  primaryColor: Color.fromARGB(201, 252, 163, 193), // Matches the app bar gradient color
  hintColor: Colors.deepPurple, // Secondary button color
  scaffoldBackgroundColor: Color.fromRGBO(18, 18, 18, 1), // Dark background
  cardColor: Color.fromRGBO(28, 28, 28, 1), // Dark card background
  textTheme: TextTheme(
    bodyLarge: TextStyle(color: Colors.white), // Default text color
    bodyMedium: TextStyle(color: Colors.grey[400]), // Secondary text
    titleLarge: TextStyle(color: Colors.white), // AppBar title color
  ),
  inputDecorationTheme: InputDecorationTheme(
    filled: true,
    fillColor: Color.fromRGBO(34, 34, 34, 1), // Text field background
    border: OutlineInputBorder(
      borderSide: BorderSide(color: Colors.grey), // Border color
    ),
    focusedBorder: OutlineInputBorder(
      borderSide: BorderSide(color: Color.fromARGB(201, 252, 163, 193)), // Focused border color
    ),
  ),
  buttonTheme: ButtonThemeData(
    buttonColor: Color.fromARGB(201, 252, 163, 193), // Primary button color
    textTheme: ButtonTextTheme.primary, // Button text color
  ),
);
*/